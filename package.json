{
    "scripts": {
        "build": "python -m venv myenv && source myenv/bin/activate && pip install -r requirements.txt && python server.py"
    },
    "dependencies": {
        "vercel": "^31.0.4"
    }
}
